### 总结

* 两条规则

  1. 只要在不同的域下面，允许同名(namespace/type/value),并且它们可以被合并为一个名字导入导出

  2. 在同一个域下面，有三种同名策略：合并(merging)、重载(overload)、覆盖(override)

* 区分“有实体的”名字空间或对象

  1. 名字空间（或模块）是否有实体，取决于它的具体声明

  2. 具名函数、类和枚举总是有实体的
