{
  "name": "learn-typescript",
  "version": "1.0.0",
  "description": "学习typescript的一个仓库",
  "author": "kz137onethe",
  "license": "ISC",
  "keywords": [],
  "main": "index.ts",
  "engines": {
    "rimraf": ">=6.0.0",
    "typescript": ">=5.6.0"
  },
  "scripts": {
    "clean": "rimraf **/node_modules --glob",
    "reinstall": "pnpm clean && pnpm i",
    "lint": "npx eslint --ignore-pattern '**/*.js'",
    "lint:fix": "npx eslint --ignore-pattern '**/*.js' --fix"
  },
  "dependencies": {
    "@learn-typescript/tsconfig": "workspace:*",
    "@learn-typescript/types": "workspace:*"
  },
  "volta": {
    "node": "24.11.1",
    "pnpm": "10.22.0"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
